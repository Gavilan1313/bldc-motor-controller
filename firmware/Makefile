# Placeholder — actualmente el proyecto compila con STM32CubeIDE managed build
# (Eclipse CDT), no con Makefile propio. Un Makefile manual se añadirá cuando
# el roadmap cubra explícitamente esa fase del toolchain.
# Ver docs/decisions/ para más contexto.
